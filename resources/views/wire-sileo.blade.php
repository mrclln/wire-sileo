<div
    id="sileo-portal"
    data-position="{{ $position }}"
    data-theme="{{ config('wire-sileo.theme', 'system') }}"
    data-fill="{{ config('wire-sileo.fill') ?? '' }}"
    data-roundness="{{ config('wire-sileo.roundness', 16) }}"
></div>
